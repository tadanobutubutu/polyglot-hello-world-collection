sub m2 { 
    if ($_[0]=~/\A([345]*)2([1-9]+)\z/) { 
        my$t=$2; 
        for my$p(reverse split//,$1) { 
            $t = 3 eq$p ? $t.2 .$t : 4 eq$p ? reverse$t : 5 eq$p ? $t x2 : die; 
        } 
        return $t; 
    } else { 
        return undef; 
    } 
}
